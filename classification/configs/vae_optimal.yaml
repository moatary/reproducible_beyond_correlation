
# Baseline VAE
model: VAE
dataset: mnist
seed: 42
n_runs: 5

hyperparameters:
  latent_dim: 16
  hidden_dim: 512
  learning_rate: 0.001
  batch_size: 64
  beta_kld: 1.0
  encoder_depth: 3
  n_epochs: 100

evaluation:
  knn_k: 50
